
@{
    ViewData["Title"] = "Statistic";
}
@section Scripts{


    <script src="~/lib/bootstrap-datepicker/dist/js/bootstrap-datepicker.js" asp-append-version="true"></script>
    <script src="~/lib/bootstrap-datepicker/dist/locales/bootstrap-datepicker.vi.min.js" asp-append-version="true"></script>
    <script src="~/app/controllers/advertisecontract/statistic.js" asp-append-version="true"></script>

    <script>
        var advertiseContractStatistic = new advertiseContractStatisticController();
        advertiseContractStatistic.initialize();</script>
}
<div class="title_left">
    <h3>Thống kê</h3>
</div>
<div class="form-group row">

    <div class="col-lg-4">
        <select class="form-control" id="selAdvertiser">
            <option value="">Chọn hãng quảng cáo</option>
        </select>
    </div>
    <div class="form-inline col-lg-4">
        <div class="input-daterange input-group" id="range-work-picker">
            <input id="dtBegin" type="text" placeholder="dd/mm/yyyy" name="start" class="form-control" />
            <span class="input-group-addon">đến</span>
            <input id="dtEnd" type="text" placeholder="dd/mm/yyyy" name="end" class="form-control" />
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_content">
                <table class="table table-striped table-bordered table-fix-header">
                    <thead>
                        <tr>
                            <th>Người quảng cáo</th>        
                            <th>Số hợp đồng đăng ký</th>
                            <th>Số hợp đồng thành công</th>
                            <th>Tổng giá trị hợp đồng<br>đã thanh toán<br>(VNĐ)</th>
                            <th>Tổng giá trị hợp đồng<br>đã thanh toán từ trước đến nay<br>(VNĐ)</th>
 
                        </tr>
                    </thead>
                    <tbody id="tbl-content"></tbody>
                </table>
                <!--Pagination-->
                <div class="row">
                    <div class="col-sm-5">
                        <div class="dataTables_info" id="datatable-checkbox_info" role="status" aria-live="polite">
                            <select id="ddlShowPage">
                                <option value="10" selected="selected">10</option>
                                <option value="20">20</option>
                                <option value="30">30</option>
                                <option value="50">50</option>
                            </select>
                            <span class="item-per-page">
                                bản ghi/trang.
                            </span>
                            Tổng số bản ghi: <strong id="lblTotalRecords"></strong>
                        </div>
                    </div><div class="col-sm-7">
                        <div class="dataTables_paginate paging_simple_numbers" id="datatable-checkbox_paginate">
                            <ul id="paginationUL"></ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script id="table-template" type="x-tmpl-mustache">
    <tr>
        <td style="text-align:center">{{Advertiser}}</td>
        <td style="text-align:center">{{NoContract}}</td>
        <td style="text-align:center">{{NoSuccessContract}}</td>
        <td style="text-align:center">{{TotalContractValuePeriod}}</td>
        <td style="text-align:center">{{TotalContractValue}}</td>
    </tr>
</script>